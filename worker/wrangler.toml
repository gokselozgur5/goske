name = "goske-proxy"
main = "src/index.js"
compatibility_date = "2025-01-01"

# After running `wrangler kv namespace create RATE`, paste the returned id here.
# (Worker won't deploy without this; KV stores per-IP and global rate-limit counters.)
[[kv_namespaces]]
binding = "RATE"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
